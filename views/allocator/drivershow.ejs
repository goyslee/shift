<% include ../partials/header %>
<% include ../partials/header2 %>
<div class="container">
	<div class="row">
		<div class="col-md-4 col-sm-12 col-xs-12 "></div>
		<div class="col-md-4 col-sm-12 col-xs-12 ">
			<p><a href="/allocator/allocations/allocationshow/<%= allocatorinfo._id %>"><%= allocatorinfo.name %></a></p>
			<form action="/allocator/<%= allocatorinfo.id %>/drivershow" method="POST" id="positionForm">
				<div class="form-group">
					<label>Add Reach truck driver</label>
					<select class="form-control" name="driver">
						<% foundColleague.forEach(function(colleague){ %>
						<%console.log(colleague)%>
						<% if (colleague.isIn == true && colleague.trucks == true && colleague.isAllocated == false && allocatorinfo.shift === colleague.shift) { %>
						<option  value="<%= colleague.fullName %>"><%= colleague.fullName %></option>
						<% } else { %>
						<p>There are no more Reach truck drivers avaiable for <%= allocatorinfo.name %></p>
						<% } %>
						<% }); %>
					</select>
					<hr>
					<button class="btn btn-danger btn-block blackgold ">Add Reach Truck Driver</button>
				</div>
			</form>
			<table class="table-responsive">
				<thead>
					<tr><th>Reach Truck Drivers:</th></tr>
				</thead>
				<tbody>
					<% var checkNum = 1; %>
					<% foundColleague.forEach(function(foundColleague){ %>
					<% foundDriver.forEach(function(foundDriver){ %>
					<% if (foundDriver.author.id.equals(allocatorinfo.id) && foundDriver.colleagueId === foundColleague.id) { %>
					<% checkNum++ %>
					<tr><td><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></td><td><form action="/allocator/<%= allocatorinfo.id  %>/drivershow/<%= foundDriver.id %>?_method=DELETE" method="POST"><button class="btn btn-primary btn-sm blackgold">Remove</button></form></td></tr>
					<% } %>
					<% }); %>
					<% }); %>
					<% if (checkNum < 2) { -%>
					<tr><td>No Reach Truck Driver assinged</td></tr>
					<% } %>
				</tbody>
			</table>
		</div>
		<!-- <div class="col-sm6 col-md-4 col-md-offset-4 col-sm-offset-3">
		</div> -->
		<div class="col-md-4 col-sm-12 col-xs-12 "></div>
	</div>
</div>
<% include ../partials/footer %>