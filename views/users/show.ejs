<% include ../partials/header %>
<% include ../partials/header2 %>
<div class="container">
    <div class="row">
        <div class="col-md-3">
            <h1 class="well"><%= user.firstName + " " + user.lastName %></h1>
            <p class="">Registration date: <%= user.time.toDateString() + " " + user.time.toLocaleTimeString() %></p> %> <% /*22:48*/ %>
        </div>
        <div class="col-md-3">
            <h1><%= user.role %></h1>
        </div>
    </div>
    <div class="col-md-3"></div>
    <% if (!currentUser) { %>
    <% } else {%>
    <% if (currentUser.isSuperAdmin) { %>
    <div class="col-md-6">
        <hr class="style-four">
        <form id="delete" class="form-group" action="/users/<%= user._id %>?_method=DELETE" method="POST">
            <button class="btn btn-primary btn-block goldblack">Delete</button>
        </form>
        <% } %>
        <% } %>
    </div>
    <div class="col-md-3">
    </div>
</div>
<!--<div class="col-md-8">-->
<!--</div>-->
</div>
<% include ../partials/footer %>